---
created: 2026-05-11
tags: #日记 #2026-05 #2026
modified: 2026-05-11
---

# 2026年05月11日 日记

**星期**：星期一

---

## 今日项目工作

> 规则：每个项目单独成块，标题使用 `## [[项目名]]`，后续每周按项目归档到对应项目的 `worklog/`。

## [[CtnmarketWeb]]

### ctngencargo 模块重构

- 将 `request.js` 中的 API 函数按业务拆分到 `api/customer.js`、`api/breakbulk.js`、`api/container.js`，`request.js` 只保留公共工具接口
- 提取通用字典、审核状态、路由路径等常量到 `constants.js`
- 提取表格列配置到 `customer-list/columns.js`，支持 CMM/BB 两种业务类型列定义
- 提取权限校验逻辑到 `customer-list/permissions.js`
- 提取查询参数构建、日期范围、地区参数到 `customer-list/query-model.js`
- 提取路由跳转逻辑到 `customer-list/route-actions.js`
- 提取错误处理工具到 `utils/error.js`
- 抽离 `ContainerBaseForm` 组件，统一 `base_info_add.vue` 和 `base_info_edit.vue` 的表单代码
- 为 `ContainerBaseForm` 添加 `formData` 计算属性代理，避免直接修改 prop
- 清理已废弃的内联表单模板，减少约 65% 的重复代码
- 统一变量命名（`customerInfo` → `formData` in ContainerBaseForm）

### 环境配置

- 切换 `.env.development` 的 API 地址到测试环境

---

**⚠️ 重要：禁止在日志中记录 git 提交信息**

- ❌ 不要写：commit hash、Conventional Commits 类型标签（feat/fix 等）
- ❌ 不要写：`.gitignore` 变更、提交辅助信息
- ✅ 只写：做了什么功能、改了啥逻辑、效果是什么

---

## 标签

#日记 #2026-05 #2026
