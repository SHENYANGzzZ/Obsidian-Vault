# water-web 开发日志

**日期**：2026-04-09
**项目**：water-web
**标签**：#dev-log #water-web #home-page-1 #arcgis #管线样式 #视觉优化

---

## 今日计划

### 核心任务

- [x] 首页管线颜色调整
- [x] GIS 管线颜色调整

---

## 今日完成

### ArcGIS 管线与流向动画样式调整

- [x] 将 ArcGIS 静态主管道、支管道、小支管道颜色切换为黄绿红正色，便于直接观察分级效果
- [x] 将流向动画粒子颜色调整为更柔和的浅蓝系，降低“电流感”
- [x] 将流向动画粒子形状由圆点改为沿流向拉伸的水滴/流梭形，提升“水在管道中流动”的视觉感受
- [x] 提交并推送代码到远程分支 `dev_gisChange`

### 变更文件

- `src/views/business/arcgis/maphook.ts`
- `src/views/business/arcgis/flow-animation/config.ts`
- `src/views/business/arcgis/flow-animation/FlowLineLayer.ts`

### Git 记录

- Commit: `c49c459`
- Message: `fix(arcgis): 调整管线与流向动画样式`

## 今日完成

### 首页地图管线粒子动画密度优化

- [x] 梳理 `home-page-1` 对 `FlowLineLayer` 的复用关系，确认首页仅负责组装管线与传参，动画实现仍由 `src/views/business/arcgis/flow-animation` 统一处理
- [x] 在流向动画模块中新增屏幕空间密度抽稀配置 `densityTuning`，用于按局部拥挤度调节粒子间距、尺寸和透明度
- [x] 改造 `FlowLineLayer`，在 `resize`、`extent` 和 `updatePolylines` 时缓存屏幕路径与密度等级，避免每帧重复计算
- [x] 保持全部管线继续显示动画，不做线裁剪，仅在密集区域降低粒子视觉密度，缓解缩小时管线挤在一起导致的粒子堆叠问题
- [x] 运行 `pnpm typecheck` 验证通过

### 变更文件

- `src/views/business/arcgis/flow-animation/types.ts`
- `src/views/business/arcgis/flow-animation/config.ts`
- `src/views/business/arcgis/flow-animation/FlowLineLayer.ts`

## 结果说明

- 静态管线分级颜色更直接，便于对照主管道、支管道、小支管道
- 流向动画相比之前更接近水流，而不是发光粒子沿线移动
- 本次先聚焦视觉效果，未调整速度、间距、数量和数据逻辑
- 首页地图在缩小视图下，密集管线区域的粒子堆叠会被主动抽稀；放大后仍保留完整动画细节
- 当前方案不隐藏任何线，后续如果需要进一步压性能，可以继续给密度缓存增加节流或 stationary 后重算策略

## 今日完成

### 首页与 ArcGIS 缓存兼容性修复

- [x] 排查 `home-page-1` 与普通菜单页在布局层的差异，确认新首页会走专用容器分支，普通菜单页走 `el-scrollbar` 分支
- [x] 确认缓存失效根因不是 `arcgis` 组件名或 `keepAlive` 配置，而是原有布局在分支切换时会连同缓存宿主一起卸载
- [x] 将 `src/layout/components/lay-content/index.vue` 改造成“三套常驻容器 + 单一 Teleport 内容宿主”结构
- [x] 保留首页大屏原生滚动容器与普通菜单 `el-scrollbar` 的差异化行为，不再因为切到首页而销毁普通菜单缓存树
- [x] 修正 Teleport 挂载点，改用真实 DOM `ref` 并在容器挂载后再启用传送，解决菜单页面短暂空白问题
- [x] 运行 `pnpm typecheck` 验证通过

### 变更文件

- `src/layout/components/lay-content/index.vue`

## 结果说明

- `arcgis -> 普通菜单 -> arcgis` 的原有缓存行为保持不变
- `arcgis -> homePage1 -> arcgis` 现在不会因为首页专用容器分支切换而丢失缓存
- 首页仍然保留自身滚动容器，不会被重新包进通用 `el-scrollbar`
- 本次修复只动布局层，不改 ArcGIS 业务逻辑和首页业务数据逻辑
