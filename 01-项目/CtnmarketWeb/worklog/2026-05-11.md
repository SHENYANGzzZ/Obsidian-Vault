---
created: 2026-05-11
updated: 2026-05-11
tags: [CtnmarketWeb, ctngencargo, 工作日志]
project: CtnmarketWeb
---

# 2026-05-11 工作日志

## 今日完成

### ctngencargo 客户维护交互修复

- [x] 检查集装箱基础资料查看入口，确认列表页已通过路由参数传入 `mode=view`
- [x] 修复 `container/base_info_edit.vue` 未消费 `mode=view` 的问题
- [x] 查看模式下隐藏保存按钮，仅保留返回按钮
- [x] 查看模式下禁用基础资料表单和联系人表格输入控件
- [x] 查看模式下隐藏联系人新增 / 删除操作列，并在新增、删除、保存方法内增加只读保护

### 集装箱新增页命名整理

- [x] 将 `container/base_info.vue` 更名为 `container/base_info_add.vue`
- [x] 同步更新路由动态 import
- [x] 同步更新 `ctngencargo/api/request.js` 中相关说明注释
- [x] 保留原路由 `/ctngencargo/baseinfo`，避免影响已有跳转入口

### 删除确认框取消误报处理

- [x] 修复 `ctngencargo/cust_table.vue` 删除确认框取消 / 关闭后误报删除失败的问题
- [x] 同步处理件杂货客户删除确认逻辑
- [x] 同步处理件杂货服务记录删除确认逻辑
- [x] 同步处理相关部门走访记录删除确认逻辑
- [x] 增加字符串错误兼容，避免确认框 reject 字符串时错误处理异常

### 重构前评估

- [x] 梳理 `src/views/ctngencargo` 当前结构和文件规模
- [x] 确认主要复杂度集中在 `cust_table.vue`、`base_info_add.vue`、`base_info_edit.vue`、`bb_cust_edit.vue`
- [x] 形成 `ctngencargo` 结构重构计划

### ctngencargo 模块重构

- [x] 将 `request.js` 中的 API 函数按业务拆分到 `api/customer.js`、`api/breakbulk.js`、`api/container.js`，`request.js` 只保留公共工具接口
- [x] 提取通用字典、审核状态、路由路径等常量到 `constants.js`
- [x] 提取表格列配置到 `customer-list/columns.js`，支持 CMM / BB 两种业务类型列定义
- [x] 提取权限校验逻辑到 `customer-list/permissions.js`
- [x] 提取查询参数构建、日期范围、地区参数到 `customer-list/query-model.js`
- [x] 提取路由跳转逻辑到 `customer-list/route-actions.js`
- [x] 提取错误处理工具到 `utils/error.js`
- [x] 抽离 `ContainerBaseForm` 组件，统一 `base_info_add.vue` 和 `base_info_edit.vue` 的表单代码
- [x] 为 `ContainerBaseForm` 添加 `formData` 计算属性代理，避免直接修改 prop
- [x] 清理已废弃的内联表单模板，减少重复代码
- [x] 统一 `ContainerBaseForm` 内部变量命名，将 `customerInfo` 整理为 `formData`

### 环境配置

- [x] 切换 `.env.development` 的 API 地址到测试环境

## 验证情况

- [x] 多次运行 `yarn build` 通过
- [x] 构建仅出现项目既有的 Vue deep 选择器弃用警告和 chunk size 警告

## 变更范围提醒

当前代码仓库仍有多处未提交变更。重构前建议先提交当前功能修复版本，作为回退基线。

建议提交说明：

```text
fix: 优化 ctngencargo 客户维护查看与删除交互
```

## 后续计划

详见：[[2026-05-11-ctngencargo-refactor-plan]]
